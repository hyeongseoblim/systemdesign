# 콘텐츠 확장 설계

## 1. 목적과 현재 기준선

이 문서는 학습 카드 수를 늘리되 같은 주제를 반복 생성하거나, 카드 품질이 낮아지는 문제를 막기 위한 운영 설계다. 숫자는 **소스 저장소 기준 2026-08-20**이며, 실제 배포 DB의 발행 수와는 다를 수 있다.

| 항목 | 현재 | 의미 |
|---|---:|---|
| 수동 발행 카드 | 100개 | `apps/api/src/main/resources/content/*.md`의 프론트매터가 정상일 때 `ContentSeeder`가 적재하는 수 |
| 수동 카드 영역 | 7개 | 시스템 디자인, 물류, 백엔드 개발·아키텍처, 데이터베이스, 인프라, CS |
| AI 생성 대기 주제 | 328개 | 마이그레이션이 만든 369개 중 수동 카드가 해결한 41개를 제외한 신규 DB 부팅 완료 기준 |
| AI 자동 생성 | 기본 비활성 | Cloud Run scale-to-zero 환경에서 Cloud Scheduler 없이 실행하면 누락·비용 통제가 어려움 |

현재 수동 카드는 시스템 디자인 24개, 물류 17개, 데이터베이스·백엔드 아키텍처·백엔드 개발 각 13개, 인프라 11개, CS 9개다. 모드는 CONCEPT 65개, DESIGN 20개, INTERVIEW 13개, REVIEW 2개다.

## 2. 목표

### 첫 번째 이정표: 검수된 수동 카드 100개

초기 수동 카드 59개에 41개를 추가해 목표를 달성했다. AI 카드와 달리 이 묶음은 핵심 면접 주제를 사람이 검수한 기준 콘텐츠로 삼는다.

| 영역 | 현재 | 추가 | 목표 |
|---|---:|---:|---:|
| 시스템 디자인 | 24 | 0 | 24 |
| 물류 | 17 | 0 | 17 |
| 백엔드 아키텍처 | 13 | 0 | 13 |
| 백엔드 개발 | 13 | 0 | 13 |
| 데이터베이스 | 13 | 0 | 13 |
| 인프라 | 11 | 0 | 11 |
| CS | 9 | 0 | 9 |
| **합계** | **100** | **0** | **100** |

전체 추가 41개는 CONCEPT 20개, DESIGN 13개, INTERVIEW 6개, REVIEW 2개로 완성했다. 기존 카드와 합친 현재 분포는 CONCEPT 65개, DESIGN 20개, INTERVIEW 13개, REVIEW 2개다.

### 두 번째 이정표: 52주 커리큘럼의 관리 가능한 공급

52주 계획의 364개와 기존 대기 5개는 한 번에 발행하지 않는다. 수동 100개를 기준 세트로 확정한 뒤, 남은 커리큘럼을 주당 3~7개씩 생성·검수한다. 자동 생성 카드는 품질 점수만으로 공개하지 않고 중복·사실성·형식 검수를 통과해야 한다.

## 3. 우선순위와 첫 배치

처음 추가할 12개는 이미 V4 커리큘럼의 초반에 있고, 기존 카드의 빈틈을 보완하는 주제다. 12개 모두 작성·검수를 마쳤고, 수동 카드가 해당 커리큘럼 항목을 `MANUAL`로 해결한다.

| 순서 | 영역 | 모드 | 주제 |
|---:|---|---|---|
| 1 | 시스템 디자인 | CONCEPT | LSM-Tree vs B+Tree 저장 엔진 ✅ |
| 2 | 물류 | CONCEPT | 주문 프로미스: ATP·CTP·컷오프·배송약속 계산 ✅ |
| 3 | 백엔드 아키텍처 | CONCEPT | Aggregate 설계 규칙과 트랜잭션 경계 ✅ |
| 4 | 데이터베이스 | CONCEPT | 인덱스 접근 최적화: Index Merge·Skip Scan·ICP·MRR ✅ |
| 5 | 시스템 디자인 | CONCEPT | 복제 프로토콜: 리더 기반·체인 복제·CRAQ ✅ |
| 6 | 백엔드 개발 | CONCEPT | JVM 메모리 구조: Heap·Metaspace·TLAB·Off-heap ✅ |
| 7 | 인프라 | CONCEPT | Kubernetes 네트워킹: Service·Ingress·CNI·kube-proxy ✅ |
| 8 | 시스템 디자인 | CONCEPT | 분산 시계: TrueTime·HLC·외부 일관성 ✅ |
| 9 | 물류 | CONCEPT | 재고 원장: 이중기입·트랜잭션 로그·잔액 스냅샷 ✅ |
| 10 | 백엔드 아키텍처 | CONCEPT | Aggregate 참조: ID 참조·객체 참조·최종 일관성 ✅ |
| 11 | 데이터베이스 | CONCEPT | 인덱스 쓰기 비용: 쓰기 증폭·페이지 분할·리빌드 ✅ |
| 12 | 백엔드 개발 | CONCEPT | Garbage Collector: G1·ZGC·STW·Region ✅ |

## 4. 중복 방지 모델

### 문제

현재 `curriculum_topics`에는 제목, 영역, 모드, `generated` Boolean만 있다. 수동 카드가 나중에 같은 주제를 다뤄도 커리큘럼 항목과 연결되지 않으므로 AI 생성 후보로 남는다. 또한 `generated=true`는 초안(DRAFT)과 발행(PUBLISHED)을 구분하지 못한다.

### 목표 모델

다음 스키마 변경을 카드 대량 작성 전에 적용한다. 기존 `generated`와 `generated_card_id`는 호환 기간 동안 유지하고, 새 상태가 기준값이 된다.

| 필드 | 예시 | 용도 |
|---|---|---|
| `topic_key` | `system-design-101` | 제목이 아니라 마이그레이션에서 고정한 전역 고유 키. 커리큘럼·수동 카드·AI 카드를 연결 |
| `resolution_status` | `PENDING`, `MANUAL`, `AI_DRAFT`, `AI_PUBLISHED`, `SKIPPED` | 주제의 실제 처리 상태 |
| `resolved_card_id` | 카드 UUID | 주제를 해결한 카드. 수동·AI 모두 연결 가능 |
| `supersedes_topic_key` | 선택값 | 더 넓은 카드가 기존 주제를 대체했을 때 추적 |

V5 마이그레이션과 애플리케이션 연동은 구현됐다. 전환·운영 절차는 다음과 같다.

1. Flyway V5가 위 필드와 제약을 추가한다. 기존 주제는 `<area>-<display_order 3자리>` 키로 한 번 고정하며, 이후 순서를 바꿔도 키를 변경하지 않는다.
2. 생성 쿼리는 `resolution_status=PENDING`만 선택한다. AI 품질 미달이면 `AI_DRAFT`, 통과하면 `AI_PUBLISHED`로 기록한다.
3. 수동 시더는 선택적인 `topicKey` 프론트매터를 읽고, 카드 생성 뒤 커리큘럼 주제를 `MANUAL`로 같은 트랜잭션에서 해결한다. 기존 slug를 만나도 누락된 연결을 보강하며, 키가 없으면 기존처럼 카드만 적재한다.
4. 기존 59개와 V2·V4를 대조한 결과, V3에서 제거한 7개 외에는 완전히 같은 범위의 주제가 없었다. 기존 종합 카드를 더 좁고 깊게 파고드는 항목은 대기 상태로 유지한다.

이 모델을 적용하기 전에는 새 수동 카드의 제목을 V4 목록과 수동 대조하고, 대응 주제를 AI 후보에서 제외하는 마이그레이션을 함께 작성한다.

## 5. 카드 제작·검수 흐름

```
커리큘럼 백로그 선정
        ↓
카드 명세(topic_key·난이도·모드·핵심 질문) 작성
        ↓
Markdown 카드 작성 → 정적 린트 → 로컬 시드
        ↓                         ↓
커리큘럼 상태 해결 ← 내용·Mermaid·모바일 화면 검수
        ↓
발행 및 다음 배치 선정
```

### 정적 린트

`scripts/check-content.sh`를 추가해 PR과 로컬에서 다음을 검사한다.

- slug와 `topicKey`의 중복, 필수 프론트매터, 영역·모드 enum, 난이도 1~5
- 질문 정확히 3개, 태그 2~6개, 제목·요약·본문의 공백 여부
- Mermaid 코드펜스 1개 이상, 비교 표 1개 이상, callout 1개 이상, 언어 지정 코드블록 1개 이상
- 링크 형식, 깨진 내부 링크, frontmatter YAML 파싱
- 커리큘럼 항목을 해결하는 수동 카드에 `topicKey`가 있는지

구조·메타데이터 계약은 전체 카드에 강제한다. 기존 33개 카드의 언어 지정 코드블록 부채를 정리했으며, 이제 Mermaid 외 코드블록 누락도 검사 실패로 처리한다.

### 사람 검수 기준

- **사실성**: 버전 의존 기술은 공식 문서 또는 신뢰할 수 있는 1차 출처 링크를 카드 끝에 둔다.
- **학습성**: 정의만 나열하지 않고 문제 상황, 트레이드오프, 면접 질문 3개를 제공한다.
- **실행 가능성**: 숫자·용량·장애 시나리오는 가정을 명시하고, 물류 사례는 보편적 원리와 특정 기업 사실을 구분한다.
- **표현 품질**: 모바일 화면에서 표가 과도하게 넓지 않은지, Mermaid가 실제 렌더되는지 확인한다.

## 6. 문서 책임 분리

| 문서 | 단일 책임 |
|---|---|
| `README.md` | 프로젝트 개요, 현재 소스 기준 카드 수, 실행 진입점 |
| `CLAUDE.md` | 콘텐츠 작성 규칙과 저장소 작업 규약 |
| `docs/local-dev.md` | 로컬 실행과 실제 기본 환경 변수 |
| `infra/README.md`, `infra/cloudrun/README.md` | Cloud Run·Neon·Vercel 운영 절차 |
| 이 문서 | 카드 수 목표, 커리큘럼 상태 모델, 제작·검수 운영 |

카드 수나 도구 버전을 바꿀 때는 위 문서 중 해당 책임을 가진 문서를 같은 변경에서 갱신한다. README에 배포 세부 명령이나 커리큘럼 전체 목록을 중복하지 않는다.

## 7. 구현 순서와 완료 조건

1. [x] V5 상태 모델과 `ContentSeeder`의 `topicKey` 연동을 구현하고 테스트한다.
2. [x] 기존 59개와 369개 대기 주제를 대조해 중복·심화 항목을 구분한다.
3. [x] 콘텐츠 린터를 도입하고 기존 카드의 형식 부채를 경고로 목록화한다.
4. [x] 위 첫 배치 12개를 작성·검수·발행한다.
5. [x] 41개 확장분을 영역·모드 배분에 맞춰 반복하고, 수동 카드 100개를 달성한다.
6. [ ] Cloud Scheduler, 비용 한도, 초안 검수 절차가 준비된 뒤에만 AI 대기열을 활성화한다.

완료는 단순 파일 수가 아니라 다음을 모두 만족할 때로 정의한다: 수동 발행 100개, 각 카드의 커리큘럼 연결 또는 연결 제외 사유, 신규 카드 린트 통과, 로컬 시드·API 피드·모바일 렌더 확인, 문서의 수치와 설정 일치.
